% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_field.R
\name{prepare_spatial_field_list}
\alias{prepare_spatial_field_list}
\title{Prepare a List of Spatial Fields for Downscaling}
\usage{
prepare_spatial_field_list(
  field_list,
  observation_data,
  response,
  region_list = NULL,
  integration_rule = NULL,
  parallel = FALSE,
  ...
)
}
\arguments{
\item{field_list}{A list of \code{spatial_field()} objects (e.g., from \code{\link[=as_spatial_field_list]{as_spatial_field_list()}}).}

\item{observation_data}{An \code{sf} object containing the response and covariates.}

\item{response}{Character name of the response variable in \code{observation_data}.}

\item{region_list}{Optional: shared precomputed region list (applied to all fields).}

\item{integration_rule}{Optional: override the integration rule for all fields.}

\item{parallel}{Logical or integer: whether/how to parallelize integration.}

\item{...}{Additional arguments passed to \code{prepare_spatial_data()}.}
}
\value{
A list of \code{prepared_spatial_data} objects (one per field).
}
\description{
Applies \code{prepare_spatial_data()} to each spatial field in a list,
extracting the design matrix and response variable for each term.
}
\details{
This is useful for additive models where multiple spatial fields (e.g., covariates,
structured priors, or random effects) contribute to the overall linear predictor.
}
