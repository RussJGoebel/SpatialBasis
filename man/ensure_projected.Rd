% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crs_utils.R
\name{ensure_projected}
\alias{ensure_projected}
\title{Ensure an sf object is in a projected CRS using UTM zone if possible}
\usage{
ensure_projected(x, fallback_epsg = 6933, verbose = TRUE)
}
\arguments{
\item{x}{An \code{sf} or \code{sfc} object.}

\item{fallback_epsg}{Integer EPSG code to use if UTM selection fails. Defaults to EPSG:6933 (Equal Earth).}

\item{verbose}{Logical; if \code{TRUE}, print messages when conversion occurs.}
}
\value{
An \code{sf} or \code{sfc} object transformed to a projected CRS if needed.
}
\description{
Converts an sf object to a projected coordinate reference system (CRS) if it is
currently in a geographic CRS (e.g., EPSG:4326). Automatically selects an appropriate
UTM zone based on the centroid of the geometry. If the CRS is already projected,
the object is returned unchanged.
}
\examples{
x <- sf::st_as_sf(data.frame(lon = -120, lat = 35), coords = c("lon", "lat"), crs = 4326)
x_proj <- ensure_projected(x)

}
