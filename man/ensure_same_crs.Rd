% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crs_utils.R
\name{ensure_same_crs}
\alias{ensure_same_crs}
\title{Ensure Two CRS Objects Are Identical}
\usage{
ensure_same_crs(crs_a, crs_b, context = "operation")
}
\arguments{
\item{crs_a}{A coordinate reference system object created by \code{sf::st_crs()}.}

\item{crs_b}{Another CRS object created by \code{sf::st_crs()}.}

\item{context}{Optional string describing the context of the check (e.g., "integration", "evaluation").}
}
\value{
Invisibly returns \code{TRUE} if the CRSs match. Otherwise, throws an error.
}
\description{
Checks whether two CRS definitions (already computed via \code{sf::st_crs()})
are identical. If not, throws an error with contextual messaging.
}
\examples{
crs1 <- sf::st_crs(4326)
crs2 <- sf::st_crs("EPSG:4326")
ensure_same_crs(crs1, crs2, context = "prediction")  # Passes

\dontrun{
crs3 <- sf::st_crs(3857)
ensure_same_crs(crs1, crs3, context = "integration")  # Errors
}
}
