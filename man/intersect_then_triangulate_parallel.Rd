% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/region.R
\name{intersect_then_triangulate_parallel}
\alias{intersect_then_triangulate_parallel}
\title{Parallel intersection and triangulation with progress reporting}
\usage{
intersect_then_triangulate_parallel(obs_polygons, stratum_polygons)
}
\arguments{
\item{obs_polygons}{An \code{sf} object of observation polygons.}

\item{stratum_polygons}{An \code{sf} object of stratification polygons.}
}
\value{
A list of \code{region_composite} objects (or \code{NULL} for no intersections).
}
\description{
Intersects each observation polygon with stratum polygons, triangulates the result,
and wraps into \code{region_composite} objects. Uses \code{future.apply} and \code{progressr} for
parallelism and progress monitoring.
}
\details{
Requires a parallel backend set with \code{future::plan()} and activates a progress
handler with \code{progressr::handlers()}. Wrap calls in \code{with_progress()} to display updates.
}
\examples{
\dontrun{
library(future)
library(progressr)
plan(multisession)
handlers(global = TRUE)
with_progress({
  result <- intersect_then_triangulate_parallel(obs_sf, strata_sf)
})
}

}
