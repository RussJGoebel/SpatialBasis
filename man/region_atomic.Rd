% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/region.R
\name{region_atomic}
\alias{region_atomic}
\title{Create an Atomic Spatial Region (Triangle or Quadrilateral)}
\usage{
region_atomic(coords, geometry = NULL)
}
\arguments{
\item{coords}{A numeric matrix with 3 or 4 rows and 2 columns representing the polygon vertices.
Rows should be ordered counterclockwise (if possible) and represent (X, Y) pairs.}

\item{geometry}{Optional. An \code{sf} geometry object associated with the region (e.g., for area-based integration).}
}
\value{
An object of class \code{"region_atomic"} and \code{"region"} with fields:
\describe{
\item{type}{The string \code{"atomic"}.}
\item{coords}{The 3 × 2 or 4 × 2 matrix of coordinates.}
\item{geometry}{Optional associated \code{sf} geometry.}
\item{shape}{Either \code{"triangle"} or \code{"quad"} depending on \code{nrow(coords)}.}
}
}
\description{
Constructs a region object representing a simple polygon (typically a triangle or quadrilateral)
used for spatial integration. Automatically infers the shape based on the number of coordinates.
}
\examples{
coords <- matrix(c(0, 0, 1, 0, 0, 1), ncol = 2, byrow = TRUE)
r <- region_atomic(coords)
str(r)

}
